export default function ConfigPage() {
  return (
    <div className="fade-in">
      <h1 style={{ marginBottom: '2rem', fontSize: '2.5rem' }}>Configuration</h1>
      <div className="glass" style={{ padding: '3rem', borderRadius: '24px', textAlign: 'center', background: 'rgba(255,255,255,0.03)' }}>
        <p style={{ color: 'var(--foreground-muted)', fontSize: '1.2rem' }}>
          This page is currently empty as requested.
        </p>
      </div>
    </div>
  );
}
